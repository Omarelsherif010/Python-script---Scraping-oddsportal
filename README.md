# Python-script---Scraping-oddsportal

steps to run the code
- create virtual envirnment and activate it `python -m venv venv`
- run `pip install -r requirements.txt`
- run the scraper.py from src directory